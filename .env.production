# Production - remotes are deployed under path prefixes on the same.
# app domain (per their nginx.conf `location /fixed/` and `/order/` blocks).
VITE_REMOTE_FIXED_URL=https://next-cimnewuat.etisalat.corp.ae/fixed
VITE_REMOTE_ORDER_URL=https://next-cimnewuat.etisalat.corp.ae/order
